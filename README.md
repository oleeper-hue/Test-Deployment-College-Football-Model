## Process

> **Step 1**  
> Upload `model.pkl`, `data_info.pkl`, and `app.py` from the Colab scripts and commit them to the repo.

> **Step 2**  
> Write a `requirements.txt` file and commit it to the repo.

> **Step 3**  
> Host app on Streamlit.
